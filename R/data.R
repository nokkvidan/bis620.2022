#' Kaggle Diabetes Health Indicator Data
#'
#' A subset of the Kaggle diabetes data where:
#'
#' `Diabetes_binary` a binary 0/1 for diabetes;
#' `HighBP` a binary 0/1 for having high blood ;
#' `HighChol` a binary 0/1 for having high cholesterol;
#' `CholCheck` a binary 0/1 if they had a cholesterol check in the last 5 years;
#' `BMI` a continuous variable for the patient's BMI;
#' `Smoker` a binary 0/1 for if they are a smoker;
#' `Stroke` a binary 0/1 for if they have had a stroke;
#' `HeartDiseaseorAttack` a binary 0/1 for if they have had a heart attack or
#' heart diesase;
#' `PhysActivity` a binary 0/1 for having worked out in the last 30 days;
#' `Fruits` a binary 0/1 for consuming 1 or more fruits a day ;
#' `Veggies` a binary 0/1 for comsuming 1 or more vegetables a day ;
#' `HvyAlcoholConsump` a binary 0/1 for 14 or more drinks a week for men or 7 or
#' more drinks a week for women;
#' `AnyHealthcare` a binary 0/1 for whether or not they have health coverage ;
#' `NoDocbcCost` a binary 0/1 for if they needed to visit the doctor within the
#' last 12 months but could not because of cost;
#' `GenHlth` a 1-5 scale for bad to good general health;
#' `MentHlth` a 1-30 continuous variable for number of days for which there has
#' been a mental health problem;
#' `PhysHlth` a 1-30 continuous variable for number of days for which there has
#' been a physical health problem;
#' `DiffWalk` a binary 0/1 for difficulty walking or using stairs;
#' `Sex` a binary 0/1 for sex where 0 is female;
#' `Age` a continuous variable for age;
#' `Education` a 1-6 scale for the level of highest education completed;
#' `Income` a 1-8 scale for the patient's income bracket;
#' @name diabetes
#' @docType data
#' @keywords data
NULL
